/**
 * @format
 */

import {AppRegistry} from 'react-native';
import App from './App';
import File from './NavigationFile'
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => File);
